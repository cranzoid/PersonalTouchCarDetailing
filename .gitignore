node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# env & secrets — never commit
.env
.env.*
!.env.example

# local uploads (customer files are private; prod uses object storage)
var/

# misc
.DS_Store
*.pem
npm-debug.log*
coverage/

# Terraform local working data and uncommitted variable files. Production
# state is stored in the Azure Storage backend configured during init.
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
terraform.tfvars
*.auto.tfvars
crash.log
